import 'package:flutter/material.dart';

class FreshProductPage extends StatelessWidget {
  const FreshProductPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Product Page'),
    );
  }
}
