import 'package:flutter/material.dart';

class RegularProductPage extends StatelessWidget {
  const RegularProductPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Product Page'),
    );
  }
}
